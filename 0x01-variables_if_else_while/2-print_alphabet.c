#include <stdio.h>
/**
 * main - print the alphabert in lowercase,
 * followed by anew line
 * Return: Alaways 0 (success)
 */
int main(void)
{
char ch;
for (ch = 'a'; ch ,= 'z'; ch++)
{
putchar(ch);
}
putchar('\n');
return (0);
}
